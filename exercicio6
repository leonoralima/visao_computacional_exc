import cv2

img = cv2.imread("imgexemplo.jpg")

# Selecionar região (y1:y2, x1:x2)
roi = img[20:300, 200:470]

cv2.imshow("Original", img)
cv2.imshow("Recorte", roi)
cv2.waitKey(0)
cv2.destroyAllWindows()
